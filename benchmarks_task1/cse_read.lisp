(define (f x y) (print (+ (- (+ x y) x) (read-num))))

(let ((p (pair () true)))
  (let ((x 200))
    (let ((y 300))
      (if (= x (read-num))
        (do
          (f (read-num) y)
          (f x y)
          (f x (read-num))
          (f x (read-num))
          (print (newline))
          (f x y)
        )
        (do
          (f y x)
          (print (newline))
          (f y (read-num))
          (f (read-num) x)
        )
      )
    )
  )
)
