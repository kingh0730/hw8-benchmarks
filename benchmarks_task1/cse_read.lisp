(define (f x y) (print (+ (- (+ x y) x) (read-num))))

(let ((p (pair () true)))
  (let ((x 200))
    (let ((y 300))
      (if (= x (read-num))
        (do
          (f (read-num) y)
          (f x y)
          (f x (read-num))
          (f x (read-num))
          (print (new-line))
          (f x y)
        )
        (do
          (f y x)
          (print (new-line))
          (f y (read-num))
          (f (read-num) x)
        )
      )
    )
  )
)
