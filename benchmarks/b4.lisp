(define (breakup)
    (let ((why (read-num)))
        (if (< why 9000)
            (- (read-num) why)
            (+ why (- 0 why))
        )
    )
)

(let ((happy false))
    (let ((nohappy true))
        (let ((nopurpose 9999))
            (breakup)
        )
    )
)
